apiVersion: v2
name: ollama
description: "Helm chart to deploy ollama with allminilm:l6-v2 and optionally expose it via cloudflared"
type: application
version: 0.1.1
appVersion: "0.3.5"

# Optional dependency to cloudflared chart (see hcf-coder Chart.yaml for reference)
dependencies:
- name: cloudflared
  version: 0.1.3
  repository: https://helm-repo.highcanfly.club
